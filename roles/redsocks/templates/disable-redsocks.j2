#!/usr/bin/env bash
set -x
if [[ $UID != 0 ]]; then
    echo "This must be run as root."
    exit 1
fi

/sbin/iptables -t nat -F REDSOCKS
/sbin/iptables -t nat -D PREROUTING -p tcp -j REDSOCKS
/sbin/iptables -t nat -X REDSOCKS
